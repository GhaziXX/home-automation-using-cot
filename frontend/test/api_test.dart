// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility that Flutter provides. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  // SignUp registerResp = await GetIt.I<APIServices>().register(
  //     forename: "flutter",
  //     surname: "flutter",
  //     email: "flutter@flutter1.com",
  //     password: "123456789",
  //     username: "flutter1");
  // print(registerResp.toString());

  // Login loginResp = await GetIt.I<APIServices>()
  //     .login(email: "dd@dd.d", password: "123456789");
  // print(loginResp.toString());

  // Profile profileResp = await GetIt.I<APIServices>().profile();
  // print(profileResp.authorized);

  // List<Profile> usersResp =
  //     await GetIt.I<APIServices>().listUsers(page: 3, limit: 10);
  // print(usersResp[1].id);

  // bool isConnected =
  //     await GetIt.I<APIServices>().isConnected();
  // print("connected $isConnected");

  // await GetIt.I<APIServices>().logout();

  // isConnected =
  //     await GetIt.I<APIServices>().isConnected();
  // print("connected $isConnected");

  // Sensor addResp = await GetIt.I<APIServices>()
  //     .addConnectedObject(roomId: "flutter", objectId: "fled12", pin: 13);
  // print(addResp.id);

  // String remResp = await GetIt.I<APIServices>()
  //     .removeConnectedObject(objectId: "fled12", roomId: "flutter");
  // print(remResp);

  // String stateResp = await GetIt.I<APIServices>().setStateOfConnectedObject(
  //     roomId: "flutter", objectId: "fled", state: false);
  // print(stateResp);

  // Sensor getstateResp = await GetIt.I<APIServices>()
  //     .getsetStateOfConnectedObject(roomId: "flutter", objectId: "fled");
  // print(getstateResp.value);

  // List<Room> listResp = await GetIt.I<APIServices>().listRooms(page: 0, limit: 20);
  // print(listResp[0].sensors);

  // List<Sensor> listSensors =
  //     await GetIt.I<APIServices>().listSensorsByRoom(roomId: "flutter");
  // print(listSensors[0].value);
}
